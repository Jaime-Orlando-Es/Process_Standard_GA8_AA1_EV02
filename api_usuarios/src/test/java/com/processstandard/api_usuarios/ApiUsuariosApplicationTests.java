package com.processstandard.api_usuarios; // Paquete principal del microservicio de usuarios
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

// Anotación que indica que esta clase es una prueba de Spring Boot
@SpringBootTest
class ApiUsuariosApplicationTests {

    // Método de prueba que verifica si el contexto de Spring Boot se carga correctamente
	@Test
	void contextLoads() {
		// Si el contexto no se carga correctamente, esta prueba fallará
	}
}
